# Given a binary tree, return the preorder traversal of its nodes’ values.
# implement preorder traversal as a method 
# without recursion

def preorderTraversal(A):

    stack = []
    print(self.val)

    while True:
        if self.left:
        while curr is not None:
            print self.val()
            stack.append(curr)
            curr = curr.left
        if not stack:
            return
        curr = curr.pop()
        while curr.right is None and stack:
            curr = stack.pop()
        curr = curr.right